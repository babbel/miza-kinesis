{
  "name": "miza-kinesis",
  "version": "1.0.3",
  "description": "Library to emit Events to Kinesis Events Queue",
  "main": "index.js",
  "engines": {
    "node": ">=12.14",
    "npm": ">=6.13"
  },
  "scripts": {
    "lint": "eslint . --ignore-path .gitignore",
    "test": "NODE_ENV=test mocha test",
    "test:coverage": "NODE_ENV=test nyc --reporter=lcov _mocha test",
    "build": "rm -rf node_modules && npm install --production"
  },
  "dependencies": {
    "aws-sdk": "^2.610.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-exclude": "^2.0.2",
    "eslint": "^6.8.0",
    "mocha": "^7.0.1",
    "nyc": "^15.0.0",
    "proxyquire": "^2.1.3",
    "sinon": "^8.1.1",
    "sinon-chai": "^3.4.0"
  },
  "author": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/babbel/miza-kinesis.git"
  },
  "license": "MIT"
}
