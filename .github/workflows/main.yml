name: Validate licenses
on: [push, pull_request]
jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1.0.0
      - uses: cdssnc/node-license-checker-github-action
        with:
          args: MIT,ISC,MPL-2.0
